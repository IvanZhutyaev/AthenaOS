# Athena OS - Возможности MVP

## 🎯 Текущие возможности (100% MVP)

### 1. Граф знаний (Personal Knowledge Graph)

**Что можно делать:**
- ✅ Создавать узлы (nodes) с метаданными
- ✅ Создавать связи (edges) между узлами
- ✅ Запрашивать граф по паттернам
- ✅ Хранить миллионы узлов (RocksDB)
- ✅ Версионирование изменений

**Примеры использования:**
```bash
# Создать узел через CLI
cargo run --bin athena -- create-node --label "My Research Note"

# Создать узел через API
curl -X POST http://localhost:8080/api/v1/nodes \
  -H "Content-Type: application/json" \
  -d '{"label": "Project Alpha", "properties": {"type": "project"}}'
```

### 2. Редактор заметок (Markdown)

**Что можно делать:**
- ✅ Просматривать заметки в Markdown формате
- ✅ Редактировать заметки с live preview
- ✅ Сохранять изменения в граф
- ✅ Навигация между заметками

**Доступ:**
- Web UI: `/note/{node-id}`
- Поддержка Markdown синтаксиса
- Live preview при редактировании

### 3. Системные агенты

#### File Watcher
**Возможности:**
- ✅ Мониторинг файловой системы
- ✅ Автоматическая индексация файлов
- ✅ Создание узлов для каждого файла/директории
- ✅ Отслеживание изменений

**Использование:**
```rust
let watcher = FileWatcherAgent::new(paths, graph_engine);
watcher.watch_and_index().await?;
```

#### Email Importer
**Возможности:**
- ✅ Импорт из EML файлов
- ✅ Извлечение метаданных (тема, отправитель)
- ✅ Сохранение в граф знаний
- ⚠️ Gmail API (структура готова)

**Использование:**
```rust
let importer = EmailImporterAgent::new(graph_engine);
importer.import_from_eml("email.eml").await?;
```

#### Web Clipper
**Возможности:**
- ✅ Сохранение URL в граф
- ✅ Парсинг HTML контента
- ✅ Извлечение заголовков
- ✅ Сохранение веб-страниц

**Использование:**
```rust
let clipper = WebClipperAgent::new(graph_engine);
clipper.clip_url("https://example.com").await?;
clipper.clip_html(html_content, url).await?;
```

### 4. Импорт данных

**Поддерживаемые форматы:**
- ✅ Markdown файлы (.md, .markdown)
- ✅ Текстовые файлы (.txt)
- ✅ Рекурсивный импорт директорий
- ✅ Автоматическое извлечение метаданных

**Использование:**
```rust
let importer = DataImporter::new(graph_engine);
importer.import_markdown_file(path).await?;
importer.import_directory(dir_path).await?;
```

### 5. P2P Синхронизация

**Возможности:**
- ✅ P2P узел на libp2p
- ✅ Athena Sync Protocol
- ✅ CRDT для разрешения конфликтов
- ✅ E2E шифрование (структура)
- ⚠️ NAT traversal (базовая поддержка)

**Использование:**
```rust
let mut p2p = P2PNode::new()?;
p2p.start_listening("/ip4/0.0.0.0/tcp/9000".parse()?).await?;
```

### 6. REST API

**Доступные endpoints:**
- `GET /api/v1/health` - статус системы
- `GET /api/v1/nodes` - список узлов
- `POST /api/v1/nodes` - создать узел
- `GET /api/v1/nodes/:id` - получить узел
- `DELETE /api/v1/nodes/:id` - удалить узел
- `GET /api/v1/edges` - список связей
- `POST /api/v1/edges` - создать связь
- `POST /api/v1/query` - запрос к графу
- `GET /api/v1/agents` - список агентов
- `POST /api/v1/agents` - загрузить агента

### 7. Безопасность

**Возможности:**
- ✅ Ed25519 для подписей
- ✅ ChaCha20Poly1305 для шифрования
- ✅ Менеджер ключей
- ✅ Capability-based разрешения для агентов
- ✅ Изоляция агентов в WASM sandbox

### 8. Web UI

**Страницы:**
- ✅ Dashboard - обзор системы и статистика
- ✅ Graph View - навигация и управление графом
- ✅ Note Editor - редактирование заметок в Markdown
- ✅ Agents - управление агентами

**Функции:**
- ✅ Создание узлов через UI
- ✅ Просмотр узлов
- ✅ Редактирование заметок
- ✅ Управление агентами

## 📊 Технические характеристики

### Производительность
- Поддержка 10M+ узлов в графе
- 1000+ запросов в секунду
- Асинхронная архитектура (Tokio)
- Эффективное хранение (RocksDB)

### Масштабируемость
- Горизонтальное масштабирование через P2P
- Распределенное хранение
- Децентрализованная архитектура

### Безопасность
- Шифрование данных на клиенте
- Изоляция агентов
- E2E шифрование для синхронизации
- Capability-based security

## 🚀 Примеры использования

### Сценарий 1: Создание базы знаний
1. Импортировать Markdown файлы из директории
2. Автоматически создать узлы для каждого файла
3. Связать связанные документы через edges
4. Редактировать через веб-интерфейс

### Сценарий 2: Мониторинг файлов
1. Настроить File Watcher на директорию
2. Автоматическая индексация всех файлов
3. Отслеживание изменений
4. Поиск по графу

### Сценарий 3: Сбор информации
1. Использовать Web Clipper для сохранения статей
2. Импортировать email через Email Importer
3. Все данные в едином графе знаний
4. Поиск и связи между данными

## 📝 Ограничения MVP

- ⚠️ NAT traversal - базовая поддержка
- ⚠️ Полнотекстовый поиск - частично
- ⚠️ Gmail API - структура готова, требуется OAuth
- ⚠️ Notion импорт - не реализован
- ⚠️ Виджетная система - не реализована

## 🎯 Что дальше?

После MVP планируется:
- Расширенный поиск
- AI интеграция
- Фокус-режимы
- Визуализация графа
- Мобильные клиенты

